<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/DLMain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#65AEDD"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/TBMainAct"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:minHeight="?attr/actionBarSize" />

        <!--        this code defines a FragmentContainerView that serves as the default navigation host for a specified navigation graph (nav_animal_lover) using the NavHostFragment.-->

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/NHFMain"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_ecohelper" />
        <!--        this code defines a FragmentContainerView that serves as the default navigation host for a specified navigation graph (nav_animal_lover) using the NavHostFragment.-->


        <ImageView
            android:id="@+id/imageView2"
            android:layout_width="433dp"
            android:layout_height="232dp"
            android:layout_gravity="center"
            android:alpha="0.8"
            app:srcCompat="@drawable/background" />


        <ImageButton
            android:id="@+id/NewsButton"
            android:layout_width="206dp"
            android:layout_height="168dp"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/newsicon" />

        <ImageButton
            android:id="@+id/ProgrammesButton"
            android:layout_width="206dp"
            android:layout_height="166dp"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/challengeicon" />

        <ImageButton
            android:id="@+id/OrganizationsButton"
            android:layout_width="206dp"
            android:layout_height="166dp"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/organizationicon" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:menu="@menu/menu_bottom"></com.google.android.material.bottomnavigation.BottomNavigationView>

    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/sideNav"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/menu_side" >

    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>
